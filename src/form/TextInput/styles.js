import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    root: {
        borderColor: 'gray',
        borderWidth: 1,
        borderStyle: 'solid',
        borderRadius: 25,
        width: '80%',
        marginTop: 10,
        marginBottom: 10,
    }
});

export default styles;